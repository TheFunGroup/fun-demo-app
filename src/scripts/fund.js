import { ethers } from "ethers";
import { tokens } from "../utils/tokens";
//Amount is in USDC
export const handleFundWallet = async function (wallet, amount) {
    console.log(amount)
    console.log(tokens)
    fetch("////", {

    })
    for (let i of tokens["5"]) {
        if (i.name == amount.name) {

        }
    }
}