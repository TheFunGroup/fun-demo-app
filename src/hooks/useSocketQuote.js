import { useEffect } from "react"


const params = {
    fromChainId: 137,
    toChainId: 42161,
}
export const useSocketQuote = (addr, provider) => {
    useEffect(() => {

    }, [addr, provider]);
}